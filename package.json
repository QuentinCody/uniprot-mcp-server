{
  "name": "uniprot-mcp-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "deploy:safe": "npm run validate && npm run deploy",
    "dev": "wrangler dev --port 8800",
    "format": "biome format --write",
    "lint:fix": "biome lint --fix",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types",
    "type-check": "tsc --noEmit",
    "validate": "node validate-deployment.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.13.1",
    "agents": "^0.2.13",
    "zod": "^3.25.67"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.6",
    "@types/node": "^24.1.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.22.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
